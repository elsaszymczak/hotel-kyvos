<%= simple_form_for [room, booking] do |f| %>
  <%= f.input :starts_at, as: :string, required: false, input_html: {class: "datepicker"}, label: false %>
  <%= f.submit "Confirm booking", id: "sweet-alert-trainee", class: "btn btn-primary btn-lg in_page" %>
<% end %>
